[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "biofilm-core"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "biofilm_core"
features = ["pyo3/extension-module"]
